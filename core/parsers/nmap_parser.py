# WillEye/core/parsers/nmap_parser.py

def parse(raw_output: str) -> str:
    """
    Stub parser: for now just return the raw nmap output.
    You can extend this later to turn it into JSON, tables, etc.
    """
    return raw_output
